http://localhost:3000/studio# 📋 Sanity CMS Schema Types Документация

## 📁 Структура schemaTypes

Все типы данных для Sanity CMS BEAUTY ATELIER:

```
schemaTypes/
├── index.ts                    (Основной файл экспорта)
├── home.ts                     (Главная страница + логотип)
├── service.ts                  (Услуги и прайс)
├── portfolioItem.ts            (Портфолио фото)
├── review.ts                   (Отзывы)
├── faq.ts                      (FAQ)
└── contacts.ts                 (Контакты и информация)
```

---

## 📚 Описание каждого типа

### 1. 🏠 **home.ts** - Главная страница

**Назначение:** Управление содержимым главной страницы и логотипом

**Основные поля:**
- `title` - Заголовок сайта
- `subtitle` - Подзаголовок
- `description` - Основное описание
- `logo` - Логотип сайта с alt-текстом
- `heroImage` - Главное изображение со слайдшоу
- `ctaText` - Текст кнопки призыва к действию
- `ctaUrl` - URL кнопки CTA
- `featuredServices` - Ссылки на избранные услуги
- `testimonialSection` - Настройка секции отзывов

**SEO:**
- `metaTitle` - Заголовок страницы (title tag)
- `metaDescription` - Описание для поисковиков
- `keywords` - Ключевые слова

**Пример использования:**
```typescript
const homeData = await client.fetch(`*[_type == "home"][0]`);
// Получить логотип:
const logo = homeData.logo.asset.url;
```

---

### 2. 💼 **service.ts** - Услуги и прайс

**Назначение:** Управление всеми услугами, пакетами и ценообразованием

**Основные поля:**
- `title` - Название услуги
- `slug` - URL-friendly название
- `description` - Краткое описание
- `fullDescription` - Подробное описание (блоки + изображения)
- `image` - Изображение услуги
- `category` - Категория (макияж, волосы, комбо, спецпредложения)

**Ценообразование:**
- `basePrice` - Базовая цена
- `currency` - Валюта (по умолчанию UZS)
- `discountPrice` - Цена со скидкой
- `duration` - Время выполнения в минутах
- `packages` - Массив пакетов с названием, ценой, описанием и особенностями

**Доступность:**
- `available` - Услуга доступна?
- `availableFrom` - Доступна с даты
- `availableUntil` - Доступна до даты

**Дополнительно:**
- `features` - Массив особенностей
- `tags` - Теги для поиска и фильтрации
- `displayOrder` - Порядок отображения на сайте

**Пример использования:**
```typescript
// Получить все услуги по категории
const services = await client.fetch(`
  *[_type == "service" && category == "makeup"]
  | sort(displayOrder asc)
`);

// Получить услугу со всеми деталями
const service = await client.fetch(`
  *[_type == "service" && slug.current == "${slug}"][0]
  { 
    ..., 
    packages[] { name, price, features[] }
  }
`);
```

---

### 3. 📸 **portfolioItem.ts** - Портфолио

**Назначение:** Управление портфолио фотографиями и проектами

**Основные поля:**
- `title` - Название проекта
- `slug` - URL-friendly название
- `description` - Краткое описание
- `category` - Категория (свадьба, мероприятие, editorial, портрет)
- `mainImage` - Главное изображение
- `gallery` - Галерея всех фотографий с подписями

**Сведения о проекте:**
- `services` - Ссылки на использованные услуги
- `artist` - Информация о мастере (имя, роль, instagram)
- `eventDetails` - Детали события (тип, дата, место, клиент)
- `featured` - Избранный проект?
- `displayOrder` - Порядок отображения

**SEO:**
- `metaTitle` - Заголовок для поисковиков
- `metaDescription` - Описание для поисковиков

**Пример использования:**
```typescript
// Получить все портфолио в категории "свадьба"
const portfolio = await client.fetch(`
  *[_type == "portfolioItem" && category == "bridal"]
  | sort(displayOrder asc)
  { title, slug, mainImage, description }
`);

// Получить отдельный проект с полной информацией
const item = await client.fetch(`
  *[_type == "portfolioItem" && slug.current == "${slug}"][0]
  {
    ...,
    services[]->{title, description},
    gallery[] { asset, alt, caption }
  }
`);
```

---

### 4. ⭐ **review.ts** - Отзывы

**Назначение:** Управление отзывами клиентов с рейтингами

**Основные поля:**
- `clientName` - Имя клиента
- `content` - Текст отзыва
- `rating` - Рейтинг от 1 до 5
- `clientImage` - Фото клиента

**Контекст отзыва:**
- `serviceType` - Тип услуги (макияж, волосы, комбо, другое)
- `eventType` - Тип события (свадьба, мероприятие, фотосессия, прочее)
- `service` - Ссылка на услугу
- `portfolio` - Ссылка на портфолио проект

**Статус:**
- `isVerified` - Проверенный отзыв?
- `featured` - Избранный отзыв?
- `displayOrder` - Порядок отображения

**Аналитика:**
- Рейтинг видимой в preview

**Пример использования:**
```typescript
// Получить все избранные отзывы
const reviews = await client.fetch(`
  *[_type == "review" && featured == true]
  | sort(displayOrder asc)
  { clientName, content, rating, clientImage }
`);

// Получить отзывы с рейтингом >= 4
const topReviews = await client.fetch(`
  *[_type == "review" && rating >= 4]
  | sort(rating desc)
`);

// Средний рейтинг
const avgRating = await client.fetch(`
  *[_type == "review"] | math::avg(rating)
`);
```

---

### 5 **faq.ts** - Часто задаваемые вопросы

**Назначение:** Управление FAQ-разделом

**Основные поля:**
- `question` - Вопрос
- `answer` - Ответ (может содержать текст и изображения)
- `category` - Категория (услуги, бронирование, цены, портфолио, прочее)
- `tags` - Теги для поиска

**Связи:**
- `relatedService` - Связанная услуга

**Популярность:**
- `featured` - Популярный вопрос?
- `displayOrder` - Порядок отображения
- `views` - Количество просмотров
- `helpfulCount` - Сколько нашли полезным

**Пример использования:**
```typescript
// Получить все вопросы по категории
const faqs = await client.fetch(`
  *[_type == "faq" && category == "pricing"]
  | sort(displayOrder asc)
`);

// Получить популярные вопросы
const popularFaqs = await client.fetch(`
  *[_type == "faq" && featured == true]
  | sort(views desc)
`);

// Поиск по тегам
const searchResults = await client.fetch(`
  *[_type == "faq" && "${tag}" in tags]
`);
```

---

### 6. 📞 **contacts.ts** - Контакты и информация

**Назначение:** Управление всеми контактными данными и информацией о компании

**Контакты:**
- `email` - Email адрес
- `phone` - Телефон
- `whatsapp` - WhatsApp номер
- `telegram` - Telegram (ник и URL)

**Социальные сети:**
- `instagram` - Instagram (ник и URL)
- `facebook` - Facebook URL
- `tiktok` - TikTok (ник и URL)
- `youtube` - YouTube URL

**Адрес и геолокация:**
- `street`, `city`, `country`, `zipCode`
- `latitude`, `longitude` - Для карты

**Режим работы:**
- Все дни недели с временем
- Примечание о выходных

**Команда:**
- Массив сотрудников с полной информацией:
  - `name`, `role`, `bio`
  - `image` - Фото
  - `specialization` - Массив специализаций
  - `instagram`, `email`

**Компания:**
- `siteName` - Название сайта
- `logo`, `favicon` - Логотипы
- `about` - Описание компании (текст + изображения)

**Финансы:**
- `bankDetails` - Банковские реквизиты
- `paymentMethods` - Методы оплаты

**Документы:**
- `privacyPolicy` - Политика конфиденциальности
- `termsConditions` - Условия использования

**Пример использования:**
```typescript
// Получить все контактные данные
const contacts = await client.fetch(`*[_type == "contacts"][0]`);

// Получить информацию о команде
const team = await client.fetch(`
  *[_type == "contacts"][0]
  { team[] { name, role, specialization, image } }
`);

// Получить режим работы
const workingHours = await client.fetch(`
  *[_type == "contacts"][0]
  { workingHours }
`);

// Получить социальные сети
const socials = await client.fetch(`
  *[_type == "contacts"][0]
  { socialMedia }
`);
```

---

## 🔗 Связи между типами (References)

```
home
  ├─> service (featuredServices[])
  └─> review (testimonialSection.featuredReviews[])

service
  ├─> faq (relatedService)
  └─> portfolioItem (используется в галереях)

portfolioItem
  ├─> service (services[])
  └─> contacts (для команды)

review
  ├─> service
  └─> portfolioItem

faq
  └─> service

contacts
  (главный документ - одна запись)
```

---

## ✅ Best Practices

### 1. Использование Slugs
Все основные типы имеют `slug` для создания URL-friendly путей:
```typescript
const path = `/service/${service.slug.current}`;
```

### 2. SEO
Все публичные типы должны иметь SEO-поля:
- Meta Title (60 символов)
- Meta Description (160 символов)
- Keywords

### 3. Медиа
Все изображения должны иметь:
- `alt` текст для доступности
- `hotspot` для правильного кропирования
- Разумный размер файла

### 4. Display Order
Для управления порядком на сайте используйте `displayOrder`.

### 5. Publishing
Используйте `publishedAt` для управления датой публикации.

### 6. Draft Status
Можно добавить поле `isDraft: boolean` для скрытия неопубликованного контента.

---

## 🚀 GROQ Примеры запросов

### Получить всё необходимое для главной страницы
```groq
*[_type == "home"][0] {
  title,
  logo,
  featuredServices[]->{ title, pricing.basePrice },
  testimonialSection {
    title,
    featuredReviews[]->{ clientName, rating, content }
  },
  seo
}
```

### Получить сервис с полной информацией
```groq
*[_type == "service" && slug.current == $slug][0] {
  ...,
  pricing {
    basePrice,
    discountPrice,
    duration,
    packages[]
  }
}
```

### Получить портфолио с сортировкой
```groq
*[_type == "portfolioItem" && category == $category] 
| sort(displayOrder asc) {
  title,
  slug,
  mainImage,
  services[]->,
  artist
}
```

### Получить FAQ с поиском
```groq
*[_type == "faq" && (
  $query in question ||
  $query in answer[].children[].text ||
  $query in tags[]
)] | sort(views desc)
```

---

## 📝 Интеграция с Next.js

### Установка:
```bash
npm install sanity next-sanity
```

### Конфигурация (sanity/client.ts):
```typescript
import { createClient } from 'next-sanity';

export const client = createClient({
  projectId: process.env.NEXT_PUBLIC_SANITY_PROJECT_ID,
  dataset: process.env.NEXT_PUBLIC_SANITY_DATASET,
  apiVersion: '2024-01-28',
  useCdn: true,
});
```

### Использование в компоненте:
```typescript
import { client } from '@/sanity/client';

export default async function ServicePage({ params }) {
  const service = await client.fetch(`
    *[_type == "service" && slug.current == "${params.slug}"][0]
  `);
  
  return <div>{service.title}</div>;
}
```

---

## 📊 Примеры данных

### Home Document
```json
{
  "_type": "home",
  "title": "BEAUTY ATELIER",
  "subtitle": "Премиальные образы для свадеб и мероприятий",
  "description": "Макияж и причёски от топ-мастеров...",
  "logo": { "asset": { "url": "..." } },
  "ctaText": "Записаться на консультацию",
  "ctaUrl": "https://t.me/beauty_atelier"
}
```

### Service Document
```json
{
  "_type": "service",
  "title": "Свадебный макияж",
  "slug": { "current": "svadebniy-makyazh" },
  "category": "makeup",
  "pricing": {
    "basePrice": 500000,
    "currency": "UZS",
    "duration": 120,
    "packages": [
      {
        "name": "Стандарт",
        "price": 500000,
        "features": ["...", "..."]
      }
    ]
  }
}
```

---

## 🔗 Полезные ссылки

- [Sanity Documentation](https://www.sanity.io/docs)
- [GROQ Documentation](https://www.sanity.io/docs/groq)
- [Sanity TypeScript SDK](https://www.sanity.io/docs/client-libraries/js-client)

---

**Версия:** 1.0.0  
**Дата:** January 28, 2026  
**Status:** ✅ Production Ready
