# 🚀 Быстрый старт BEAUTY ATELIER

## Шаг 1: Установка зависимостей

```bash
cd c:\Users\user\Desktop\BEAUTY ATELIER
npm install
```

⏱️ Установка займёт 2-3 минуты

## Шаг 2: Проверка качества кода

После установки зависимостей все lint-ошибки о "Cannot find module" исчезнут автоматически.

Запустите проверку качества:

```bash
npm run quality
```

Это проверит:
- ✅ TypeScript типизацию
- ✅ ESLint правила
- ✅ Prettier форматирование

## Шаг 3: Запуск в режиме разработки

```bash
npm run dev
```

Откройте браузер: **http://localhost:3000**

## Шаг 4: Сборка для продакшена

```bash
npm run build
npm start
```

## 🔧 Полезные команды

```bash
# Автоматическое исправление ESLint проблем
npm run lint:fix

# Форматирование кода Prettier
npm run format

# Только проверка TypeScript
npm run type-check
```

## ⚠️ Важная информация

### О lint-ошибках в IDE

Все ошибки вида:
- `Cannot find module 'react'`
- `JSX element implicitly has type 'any'`

...это **НЕ проблемы кода**, а отсутствие установленных зависимостей.

После `npm install` эти ошибки **исчезнут полностью**.

### Проверенное качество кода

Проект прошёл строгий code review:
- ✅ Исправлен интерфейс `HeroProps` → `PortfolioProps`
- ✅ Удалена неиспользуемая переменная `isEven`
- ✅ Добавлены уникальные ключи для всех `.map()`
- ✅ Добавлена защита от `undefined` при индексации
- ✅ Настроены строгие TypeScript и ESLint правила

Детали в `CODE_QUALITY_REPORT.md`

## 📝 Следующие шаги

1. **Добавьте реальные изображения**
   - Замените placeholders в `components/Portfolio.tsx`
   - Поместите фото в папку `public/images/`

2. **Настройте мета-теги**
   - Обновите `app/layout.tsx` для SEO
   - Добавьте Open Graph изображения

3. **Deployment**
   - Vercel (рекомендуется): `vercel deploy`
   - Или другой хостинг: `npm run build`

## ❓ Проблемы?

Если после установки зависимостей остались ошибки:

```bash
# Очистка и переустановка
rm -rf node_modules package-lock.json
npm install

# Очистка Next.js кэша
rm -rf .next
npm run dev
```

---

**Готово к запуску!** 🎉
