# 🔐 Панель администратора BEAUTY ATELIER

## 📋 Описание

Полнофункциональная панель администратора для управления сайтом BEAUTY ATELIER с аутентификацией, управлением контентом и настройками.

---

## 🚀 Быстрый старт

### Доступ к панели

1. **Открыть админ-панель:**
   - Перейти на `http://localhost:3000/admin`
   - или `http://localhost:3000/admin/login`

2. **Учетные данные (по умолчанию):**
   - **Имя пользователя:** `admin`
   - **Пароль:** `admin123`

3. **После входа:**
   - Вас перенаправит на главную панель управления
   - У вас появится доступ к разделам управления

---

## 📁 Структура файлов

```
app/
├── admin/
│   ├── page.tsx                    # Главная страница админ-панели
│   ├── layout.tsx                  # Layout для админ-раздела
│   ├── login/
│   │   └── page.tsx                # Страница входа
│   └── dashboard/
│       ├── page.tsx                # Главная панель управления
│       ├── content/
│       │   └── page.tsx            # Управление контентом
│       └── settings/
│           └── page.tsx            # Настройки сайта
├── api/
│   └── admin/
│       └── login/
│           └── route.ts            # API endpoint для входа

components/admin/
├── ProtectedRoute.tsx              # Компонент защиты маршрутов
└── AdminNav.tsx                    # Навигация админ-панели

lib/admin/
└── auth.ts                         # Функции аутентификации
```

---

## 🔑 Функциональность

### 1. Аутентификация

- **Безопасный вход** с проверкой учетных данных
- **Сессии** сохраняются в localStorage
- **Защита маршрутов** для неавторизованных пользователей
- **Кнопка выхода** со всех страниц

### 2. Панель управления (Dashboard)

- **Статистика:**
  - Количество посещений
  - Количество клиентов
  - Количество контента
  - Статус системы

- **Последняя активность** - отслеживание изменений
- **Быстрые ссылки** на основные разделы

### 3. Управление контентом

- **Список контента** с информацией:
  - Название
  - Тип (Portfolio, Reviews, Experts)
  - Статус (опубликовано/черновик)
  - Дата создания

- **Действия:**
  - Добавить новый контент
  - Редактировать существующий
  - Удалить контент

### 4. Настройки сайта

#### Основные параметры
- Название сайта
- Описание
- Email
- Телефон

#### Социальные сети
- Telegram (ник и URL)
- Instagram

#### SEO
- SEO Title (заголовок)
- Meta Description (описание для поисковиков)

---

## 🔧 Изменение учетных данных

### Способ 1: Переменные окружения

1. Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_ADMIN_USERNAME=your_username
NEXT_PUBLIC_ADMIN_PASSWORD=your_password
```

2. Перезагрузите сервер разработки

### Способ 2: Редактирование кода

Отредактируйте файл `lib/admin/auth.ts`:

```typescript
const ADMIN_USERNAME = 'your_username';
const ADMIN_PASSWORD = 'your_password';
```

**⚠️ Важно:** Не используйте простые пароли в продакшене!

---

## 🛡️ Безопасность

### Текущие меры

- ✅ Базовая аутентификация с проверкой учетных данных
- ✅ Защита маршрутов от неавторизованного доступа
- ✅ Сессии в localStorage (только для примера)

### Рекомендации для продакшена

1. **Используйте NextAuth.js или JWT:**
   ```bash
   npm install next-auth
   ```

2. **Добавьте HTTPS:**
   - Обязательно в продакшене
   - Защищает от перехвата данных

3. **Шифруйте пароли:**
   - Не храните пароли в открытом виде
   - Используйте bcrypt или Argon2

4. **Добавьте логирование:**
   - Отслеживайте попытки входа
   - Сохраняйте логи действий админа

5. **Установите rate limiting:**
   - Ограничьте попытки входа
   - Защитите от brute-force атак

---

## 📊 Использованные технологии

- **Next.js 14** - Framework
- **React 18** - UI Library
- **TypeScript** - Type Safety
- **Tailwind CSS** - Styling
- **Lucide React** - Icons

---

## 🎨 Кастомизация

### Изменение цветов

Откройте `components/admin/AdminNav.tsx` и измените цветовые классы:

```tsx
className="bg-burgundy text-white"  // Измените на нужный цвет
```

### Добавление новых пунктов меню

В `components/admin/AdminNav.tsx`:

```tsx
const navItems = [
  { href: '/admin/dashboard', label: 'Панель управления', icon: BarChart3 },
  { href: '/admin/dashboard/content', label: 'Контент', icon: FileText },
  { href: '/admin/dashboard/settings', label: 'Настройки', icon: Settings },
  // Добавьте новый пункт:
  { href: '/admin/dashboard/users', label: 'Пользователи', icon: Users },
];
```

### Добавление новой страницы

1. Создайте новую папку в `app/admin/dashboard/`
2. Добавьте `page.tsx` с компонентом
3. Оберните в `<ProtectedRoute>` для защиты
4. Добавьте в меню `AdminNav`

Пример:

```tsx
// app/admin/dashboard/users/page.tsx
'use client';

import ProtectedRoute from '@/components/admin/ProtectedRoute';
import AdminNav from '@/components/admin/AdminNav';

export default function UsersPage() {
  return (
    <ProtectedRoute>
      <div className="min-h-screen bg-gray-50">
        <AdminNav currentPage="Пользователи" />
        {/* Ваш контент */}
      </div>
    </ProtectedRoute>
  );
}
```

---

## 🚀 Развертывание

### Vercel

1. Используйте переменные окружения:
   - Откройте Settings → Environment Variables
   - Добавьте `NEXT_PUBLIC_ADMIN_USERNAME`
   - Добавьте `NEXT_PUBLIC_ADMIN_PASSWORD`

2. Разверните как обычно:
   ```bash
   git push
   ```

### Timeweb

1. Добавьте переменные в `.env`:
   ```env
   NEXT_PUBLIC_ADMIN_USERNAME=your_username
   NEXT_PUBLIC_ADMIN_PASSWORD=your_password
   ```

2. Разверните на сервер

---

## 📝 API Endpoints

### Логин

**POST** `/api/admin/login`

```bash
curl -X POST http://localhost:3000/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

**Ответ:**
```json
{
  "success": true,
  "message": "Authenticated",
  "token": "authenticated"
}
```

---

## 🐛 Troubleshooting

### Не могу войти

- Проверьте правильность имени пользователя и пароля
- Убедитесь, что сервер работает
- Очистите кэш браузера (Ctrl+Shift+Del)
- Проверьте консоль браузера (F12) на ошибки

### Сессия потеряна после перезагрузки

- Это нормально для текущей реализации с localStorage
- Переходить на NextAuth.js для более надежного решения

### Страница администратора недоступна

- Убедитесь, что путь правильный: `/admin`
- Проверьте, что проект собран: `npm run build`
- Перезагрузите сервер разработки

---

## 📚 Дополнительные ресурсы

- [Next.js Documentation](https://nextjs.org/docs)
- [NextAuth.js для безопасности](https://next-auth.js.org/)
- [Tailwind CSS](https://tailwindcss.com/)
- [TypeScript](https://www.typescriptlang.org/)

---

## 📞 Поддержка

Для вопросов и предложений обратитесь к разработчику или создайте issue в репозитории.

---

**Версия:** 1.0.0  
**Последнее обновление:** January 2026
