# 🌐 Деплой BEAUTY ATELIER Landing на Timeweb Cloud

**Дата:** 23 января 2026  
**Платформа:** Timeweb Cloud (https://timeweb.cloud)  
**Проект:** Next.js 14 App Router

---

## 📋 Варианты размещения

### Вариант 1: Node.js Хостинг (Рекомендуется) ⭐
**Плюсы:**
- Простая настройка через панель управления
- Автоматические обновления
- Поддержка Node.js из коробки

**Минусы:**
- Может быть ограничение по ресурсам на базовых тарифах

### Вариант 2: Облачный сервер (VPS)
**Плюсы:**
- Полный контроль
- Больше ресурсов
- Возможность масштабирования

**Минусы:**
- Требуется настройка сервера
- Больше стоимость

### Вариант 3: Docker контейнер
**Плюсы:**
- Изоляция приложения
- Легко переносить между серверами

**Минусы:**
- Требуется знание Docker

---

## 🚀 Инструкция: Node.js Хостинг (Быстрый старт)

### Шаг 1: Подготовка проекта

#### 1.1 Обновите package.json
Убедитесь, что есть скрипты для production:

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint"
  }
}
```

#### 1.2 Создайте .env.production (если нужны переменные окружения)
```env
NODE_ENV=production
```

### Шаг 2: Регистрация на Timeweb Cloud

1. Перейдите на https://timeweb.cloud
2. Зарегистрируйтесь или войдите
3. Пополните баланс (минимум ~200₽/месяц)

### Шаг 3: Создание Node.js хостинга

#### 3.1 В панели управления:
1. **"Серверы"** → **"Node.js"** → **"Создать сервер"**
2. Выберите тариф (минимум: **512 MB RAM**, **Node.js 18+**)
3. Укажите имя проекта: `beauty-atelier-landing`
4. Нажмите **"Создать"**

#### 3.2 Подключение к серверу через SSH:
```bash
ssh username@your-server-ip
```
*(Данные для подключения в панели управления)*

### Шаг 4: Загрузка проекта на сервер

#### Вариант A: Через Git (Рекомендуется)

##### 4.1 Создайте Git репозиторий (если еще нет)
```bash
# На вашем компьютере (в папке проекта)
cd c:\Users\user\Desktop\BEAUTY ATELIER
git init
git add .
git commit -m "Initial commit: BEAUTY ATELIER v1.1.0"
```

##### 4.2 Загрузите на GitHub/GitLab
```bash
# Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/your-username/beauty-atelier-landing.git
git push -u origin main
```

##### 4.3 Клонируйте на сервер
```bash
# На сервере Timeweb
cd /home/your-username
git clone https://github.com/your-username/beauty-atelier-landing.git
cd beauty-atelier-landing
```

#### Вариант B: Через FTP/SFTP

1. Используйте FileZilla или WinSCP
2. Подключитесь к серверу по SFTP
3. Загрузите все файлы проекта в `/home/your-username/beauty-atelier-landing`

### Шаг 5: Установка зависимостей на сервере

```bash
# На сервере
cd /home/your-username/beauty-atelier-landing

# Установите Node.js 18+ (если не установлен)
nvm install 18
nvm use 18

# Установите зависимости
npm install

# Соберите проект
npm run build
```

### Шаг 6: Настройка PM2 (Process Manager)

PM2 - менеджер процессов для автоматического перезапуска приложения.

```bash
# Установите PM2 глобально
npm install -g pm2

# Создайте файл конфигурации
nano ecosystem.config.js
```

Вставьте следующее содержимое:

```javascript
module.exports = {
  apps: [{
    name: 'beauty-atelier-landing',
    script: 'npm',
    args: 'start',
    cwd: '/home/your-username/beauty-atelier-landing',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '500M',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    }
  }]
}
```

```bash
# Запустите приложение
pm2 start ecosystem.config.js

# Сохраните конфигурацию для автозапуска
pm2 save
pm2 startup

# Проверьте статус
pm2 status
```

### Шаг 7: Настройка Nginx (Reverse Proxy)

#### 7.1 Установите Nginx (если не установлен)
```bash
sudo apt update
sudo apt install nginx -y
```

#### 7.2 Создайте конфигурацию сайта
```bash
sudo nano /etc/nginx/sites-available/beauty-atelier
```

Вставьте:

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Оптимизация для статических файлов Next.js
    location /_next/static {
        proxy_pass http://localhost:3000;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location /images {
        proxy_pass http://localhost:3000;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
```

#### 7.3 Активируйте конфигурацию
```bash
# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/beauty-atelier /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### Шаг 8: Привязка домена

#### 8.1 В панели Timeweb Cloud:
1. **"Домены"** → **"Добавить домен"**
2. Введите ваш домен (например, `beauty-atelier.com`)

#### 8.2 Настройте DNS записи:

У вашего регистратора доменов (например, REG.RU, nic.ru):

**A-запись:**
```
Тип: A
Имя: @
Значение: IP-адрес вашего сервера Timeweb
TTL: 3600
```

**A-запись для www:**
```
Тип: A
Имя: www
Значение: IP-адрес вашего сервера Timeweb
TTL: 3600
```

⏱️ **Ожидание:** DNS изменения могут занять от 1 до 24 часов.

### Шаг 9: Установка SSL сертификата (HTTPS)

#### 9.1 Установите Certbot
```bash
sudo apt install certbot python3-certbot-nginx -y
```

#### 9.2 Получите SSL сертификат
```bash
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

#### 9.3 Следуйте инструкциям:
- Введите email для уведомлений
- Согласитесь с условиями
- Выберите опцию "2: Redirect" для автоматического перенаправления HTTP → HTTPS

#### 9.4 Автоматическое обновление сертификата
```bash
# Проверьте автообновление
sudo certbot renew --dry-run
```

Сертификат будет автоматически обновляться каждые 90 дней.

---

## 🔄 Обновление сайта

### Через Git:
```bash
# На сервере
cd /home/your-username/beauty-atelier-landing
git pull origin main
npm install
npm run build
pm2 restart beauty-atelier-landing
```

### Через FTP:
1. Загрузите обновленные файлы
2. Подключитесь по SSH
3. Выполните:
```bash
cd /home/your-username/beauty-atelier-landing
npm install
npm run build
pm2 restart beauty-atelier-landing
```

---

## 📊 Мониторинг и управление

### Просмотр логов PM2:
```bash
pm2 logs beauty-atelier-landing
```

### Перезапуск приложения:
```bash
pm2 restart beauty-atelier-landing
```

### Остановка приложения:
```bash
pm2 stop beauty-atelier-landing
```

### Проверка статуса Nginx:
```bash
sudo systemctl status nginx
```

---

## 🛡️ Безопасность

### 1. Настройте Firewall
```bash
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```

### 2. Отключите root login
```bash
sudo nano /etc/ssh/sshd_config
# Измените: PermitRootLogin no
sudo systemctl restart sshd
```

### 3. Регулярные обновления
```bash
sudo apt update && sudo apt upgrade -y
```

---

## ⚡ Оптимизация производительности

### 1. Включите Gzip в Nginx
В `/etc/nginx/nginx.conf`:
```nginx
gzip on;
gzip_vary on;
gzip_proxied any;
gzip_comp_level 6;
gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;
```

### 2. Настройте кэширование
```bash
# В PM2 конфигурации увеличьте instances для использования всех ядер
instances: 'max'
```

---

## 📞 Поддержка Timeweb Cloud

- **Документация:** https://timeweb.cloud/help
- **Поддержка:** support@timeweb.com
- **Telegram:** @timeweb_cloud

---

## ✅ Чеклист перед запуском

- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Все изображения загружены в `public/images`
- [ ] Домен привязан и DNS настроены
- [ ] SSL сертификат установлен
- [ ] PM2 настроен и приложение запущено
- [ ] Nginx настроен как reverse proxy
- [ ] Firewall настроен
- [ ] Сайт открывается по HTTPS

---

## 🎉 Готово!

Ваш сайт доступен по адресу: **https://your-domain.com**

---

## 🔧 Альтернатива: Vercel (бесплатный вариант)

Если Timeweb Cloud кажется сложным, можете использовать **Vercel** (создатели Next.js):

### Преимущества Vercel:
- ✅ Бесплатный тариф
- ✅ Автоматический деплой из Git
- ✅ Встроенный SSL
- ✅ CDN по всему миру
- ✅ Автоматическая оптимизация изображений

### Инструкция Vercel:
1. Зарегистрируйтесь на https://vercel.com
2. Подключите GitHub репозиторий
3. Нажмите **"Deploy"**
4. Привяжите свой домен в настройках проекта

**Время деплоя:** 2-3 минуты! 🚀

---

**Вопросы?** Обращайтесь за помощью!
